# Migration `20190727092157-remove-gallery`

This migration has been generated by Christian de Botton at 7/27/2019, 9:21:57 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "cdb"."Gallery";
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190727092023-create-user..20190727092157-remove-gallery
--- datamodel.dml
+++ datamodel.dml
@@ -15,10 +15,5 @@
   id       String  @default(cuid()) @id @unique
   email    String  @unique
   password String
   name     String?
-}
-
-model Gallery {
-  id    String @default(cuid()) @id @unique
-  title String
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190727092157-remove-gallery)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190727092157-remove-gallery'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
