# Migration `20190815102548-add-galleries-and-media-to-user`

This migration has been generated by Christian de Botton at 8/15/2019, 10:25:48 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Gallery" DROP COLUMN "author",ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."GalleryMedia" DROP COLUMN "gallery",ADD COLUMN "gallery" text   REFERENCES "public"."Gallery"("id") ON DELETE SET NULL,DROP COLUMN "media",ADD COLUMN "media" text   REFERENCES "public"."Media"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Media" DROP COLUMN "author",ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190814222257-initial-schema..20190815102548-add-galleries-and-media-to-user
--- datamodel.dml
+++ datamodel.dml
@@ -17,8 +17,10 @@
   deleted   Boolean   @default(false)
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
   lastLogin DateTime?
+  galleries Gallery[]
+  media     Media[]
 }
 model Gallery {
   id          String         @default(cuid()) @unique @id
```

## Photon Usage

You can use a specific Photon built for this migration (20190815102548-add-galleries-and-media-to-user)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190815102548-add-galleries-and-media-to-user'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
