# Migration `20190727092023-create-user`

This migration has been generated by Christian de Botton at 7/27/2019, 9:20:23 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "cdb"."User"("id" text NOT NULL  ,"email" text NOT NULL DEFAULT '' ,"password" text NOT NULL DEFAULT '' ,"name" text   ,PRIMARY KEY ("id"));

CREATE TABLE "cdb"."Gallery"("id" text NOT NULL  ,"title" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "User.id._UNIQUE" ON "cdb"."User"("id")

CREATE UNIQUE INDEX "User.email._UNIQUE" ON "cdb"."User"("email")

CREATE UNIQUE INDEX "Gallery.id._UNIQUE" ON "cdb"."Gallery"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190727092023-create-user
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,24 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://cdebotton@localhost:5432?schema=cdb"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id       String  @default(cuid()) @id @unique
+  email    String  @unique
+  password String
+  name     String?
+}
+
+model Gallery {
+  id    String @default(cuid()) @id @unique
+  title String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190727092023-create-user)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190727092023-create-user'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
