# Migration `20190729195729-create-user-create-user-tables`

This migration has been generated by Christian de Botton at 7/29/2019, 7:57:29 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "cdb"."User"("id" text NOT NULL  ,"email" text NOT NULL DEFAULT '' ,"password" text NOT NULL DEFAULT '' ,"lastLogin" timestamp(3)   ,PRIMARY KEY ("id"));

CREATE TABLE "cdb"."Profile"("id" text NOT NULL  ,"firstName" text NOT NULL DEFAULT '' ,"lastName" text NOT NULL DEFAULT '' ,"createdAt" timestamp(3) NOT NULL  ,"updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY ("id"));

ALTER TABLE "cdb"."User" ADD COLUMN "profile" text NOT NULL  REFERENCES "cdb"."Profile"("id");

CREATE UNIQUE INDEX "User.id._UNIQUE" ON "cdb"."User"("id")

CREATE UNIQUE INDEX "User.email._UNIQUE" ON "cdb"."User"("email")

CREATE UNIQUE INDEX "Profile.id._UNIQUE" ON "cdb"."Profile"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190729195729-create-user-create-user-tables
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,29 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://cdebotton@localhost:5432?schema=cdb"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model User {
+  id        String    @default(cuid()) @id @unique
+  email     String    @unique
+  password  String
+  profile   Profile
+  lastLogin DateTime?
+}
+
+model Profile {
+  id        String   @default(cuid()) @id @unique
+  user      User
+  firstName String
+  lastName  String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190729195729-create-user-create-user-tables)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190729195729-create-user-create-user-tables'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
