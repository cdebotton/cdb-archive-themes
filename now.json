{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@now/next" }],
  "build": {
    "env": {
      "POSTGRESQL_URL": "@postgresql-url",
      "JWT_SECRET": "@jwt-secret",
      "TOKEN_KEY": "@token-key"
    }
  }
}
